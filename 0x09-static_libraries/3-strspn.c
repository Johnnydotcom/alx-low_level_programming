#include "main.h"

/**
 * _strspn - entry point
 * @s: string to be scanned
 * @accept: containing characters to be scanned
 *
 * Return: always 0 (success)
 */

unsigned int _strspn(char *s, char *accept)
{
}
