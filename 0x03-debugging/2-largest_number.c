#include "main.h"

/**
 * largest_number - returns the largest of 2 numbers
 * @a: first integer
 * @b: second integer
 * Return: largest number
 */

int largest_number(int a, int b)
{
	int largest;

	if (a > b)
	{
	largest = a;
	else (b > a && a > c)
	{
		largest = b;
	return (largest);
}
